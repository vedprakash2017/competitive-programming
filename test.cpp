#include <bits/stdc++.h>

using  namespace std;
#define IOS ios_base::sync_with_stdio(0);cin.tie(0);cout.tie(0);

using lli = long long int;
lli n;
vector<int> temp;
void solve()
{
    cin>>n;
    for (lli i = 0; i < n; ++i)
    {
        
    }
}

signed main()
{
    IOS
    // lli t;cin>>t;while(t--)
    solve();
}