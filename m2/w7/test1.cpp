#include <bits/stdc++.h>

using  namespace std;
#define IOS ios_base::sync_with_stdio(0);cin.tie(0);cout.tie(0);

using lli = long long int;
lli n;
vector<int> temp;

lli dp[100100];

bool rec(lli lvl)
{
	if(lvl == 0 ) return 0;


 }
void solve()
{
	string s= "bed";cout<<s.substr(0,1);
//     cin>>n>>k;
//     tmep.resize(n);
//     for (lli i = 0; i < n; ++i)
//     {
//         cin>>temp[i];
//     }
//     cout<<rec(0);
}

signed main()
{
    IOS
    // lli t;cin>>t;while(t--)
    solve();
}