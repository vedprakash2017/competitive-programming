#include <bits/stdc++.h>

using  namespace std;
#define IOS ios_base::sync_with_stdio(0);cin.tie(0);cout.tie(0);

using lli = long long int;
lli n;
vector<int> temp;
string mm[8];

void fn(string b[5])

{
	cout<<b[1][0];
}
void solve()
{
    cin>>n;
    string x;
    for (lli i = 0; i < n; ++i)
    {
    	cin>>x[i];
        
    }
    fn(x);
}

signed main()
{
    IOS
    // lli t;cin>>t;while(t--)
    solve();
}